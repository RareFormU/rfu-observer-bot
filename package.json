{
  "name": "rfu-observer-bot",
  "version": "1.0.0",
  "description": "RareForm United Discord bot — whale alerts + Observer Protocol NFT gating",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy-commands": "node deploy-commands.js",
    "build": "node --check index.js && node --check verify.js && node --check revenue-service.js && node --check my-revenue.js && node --check revenue-stats.js && node --check revenue-explainer.js && node --check agent-status.js && node --check wallet-links.js && node --check deploy-commands.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0",
    "axios": "^1.6.7",
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "xrpl": "^3.0.0"
  }
}
